# UI-reference (design reference only)

> ⚠️ **These HTML pages are NOT the real frontend.**

The actual frontend lives in a **separate repository** (built by the frontend
developer, deployed on Vercel). This folder is kept purely as **design
reference** — page layouts and UI concepts from the prototype phase.

If you are building the real frontend, read
[`../FRONTEND_INTEGRATION.md`](../FRONTEND_INTEGRATION.md) instead — it documents
the actual backend API at `https://r2p-enterprise.onrender.com`.
